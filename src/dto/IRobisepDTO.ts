export default interface IRobisepDTO {
  domainId: string;
  nickname: string;
  serialNumber: string;
  code: string;
  description?: string;
  robisepTypeId: string;
  roomId: string;
  state?: string;
}
